@import: regex/types
@import: regex/compiler
@import: parser

open Parser


module Regex : sig

  val compile : string -> (match-data list) parser

end = struct

  let compile = Compiler.compile

end
