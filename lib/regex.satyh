@import: regex/types/export
@import: regex/compiler
@import: regex/exec
@import: parser

open Parser


module Regex : sig

  val compile : string -> regex
  val run : regex -> (match-data list) parser

end = struct

  let compile = Compiler.compile

  let run re = Exec.run re#inst-seq re#n-save

end
