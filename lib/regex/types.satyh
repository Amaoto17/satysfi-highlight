type match-data = (int * int * string) option % begin, end, matched string

type regex-vm = (|
  pos-stack : int list;
  matched   : match-data list;
|)

let regex-vm-init = (|
  pos-stack = [];
  matched   = [];
|)
